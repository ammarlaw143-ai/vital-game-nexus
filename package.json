{
  "name": "nexus-gaming-static",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — nothing to build.'",
    "build:dev": "echo 'Static site — nothing to build.'",
    "dev": "python3 -m http.server"
  }
}

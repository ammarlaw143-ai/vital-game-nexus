{
  "name": "nexus-gaming-static",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r *.html robots.txt assets dist/",
    "build:dev": "rm -rf dist && mkdir -p dist && cp -r *.html robots.txt assets dist/",
    "dev": "./serve.sh"
  }
}
